#[doc = r" Value read from the register"]
pub struct R {
    bits: u32,
}
#[doc = r" Value to write to the register"]
pub struct W {
    bits: u32,
}
impl super::RECYCLE_COUNT {
    #[doc = r" Modifies the contents of the register"]
    #[inline]
    pub fn modify<F>(&self, f: F)
    where
        for<'w> F: FnOnce(&R, &'w mut W) -> &'w mut W,
    {
        let bits = self.register.get();
        let r = R { bits: bits };
        let mut w = W { bits: bits };
        f(&r, &mut w);
        self.register.set(w.bits);
    }
    #[doc = r" Reads the contents of the register"]
    #[inline]
    pub fn read(&self) -> R {
        R {
            bits: self.register.get(),
        }
    }
    #[doc = r" Writes to the register"]
    #[inline]
    pub fn write<F>(&self, f: F)
    where
        F: FnOnce(&mut W) -> &mut W,
    {
        let mut w = W::reset_value();
        f(&mut w);
        self.register.set(w.bits);
    }
    #[doc = r" Writes the reset value to the register"]
    #[inline]
    pub fn reset(&self) {
        self.write(|w| w)
    }
}
#[doc = r" Value of the field"]
pub struct RECYCLE_COUNT0R {
    bits: u8,
}
impl RECYCLE_COUNT0R {
    #[doc = r" Value of the field as raw bits"]
    #[inline]
    pub fn bits(&self) -> u8 {
        self.bits
    }
}
#[doc = r" Value of the field"]
pub struct RECYCLE_COUNT1R {
    bits: u8,
}
impl RECYCLE_COUNT1R {
    #[doc = r" Value of the field as raw bits"]
    #[inline]
    pub fn bits(&self) -> u8 {
        self.bits
    }
}
#[doc = r" Value of the field"]
pub struct RECYCLE_COUNT2R {
    bits: u8,
}
impl RECYCLE_COUNT2R {
    #[doc = r" Value of the field as raw bits"]
    #[inline]
    pub fn bits(&self) -> u8 {
        self.bits
    }
}
#[doc = r" Proxy"]
pub struct _RECYCLE_COUNT0W<'a> {
    w: &'a mut W,
}
impl<'a> _RECYCLE_COUNT0W<'a> {
    #[doc = r" Writes raw bits to the field"]
    #[inline]
    pub unsafe fn bits(self, value: u8) -> &'a mut W {
        const MASK: u8 = 255;
        const OFFSET: u8 = 0;
        self.w.bits &= !((MASK as u32) << OFFSET);
        self.w.bits |= ((value & MASK) as u32) << OFFSET;
        self.w
    }
}
#[doc = r" Proxy"]
pub struct _RECYCLE_COUNT1W<'a> {
    w: &'a mut W,
}
impl<'a> _RECYCLE_COUNT1W<'a> {
    #[doc = r" Writes raw bits to the field"]
    #[inline]
    pub unsafe fn bits(self, value: u8) -> &'a mut W {
        const MASK: u8 = 255;
        const OFFSET: u8 = 8;
        self.w.bits &= !((MASK as u32) << OFFSET);
        self.w.bits |= ((value & MASK) as u32) << OFFSET;
        self.w
    }
}
#[doc = r" Proxy"]
pub struct _RECYCLE_COUNT2W<'a> {
    w: &'a mut W,
}
impl<'a> _RECYCLE_COUNT2W<'a> {
    #[doc = r" Writes raw bits to the field"]
    #[inline]
    pub unsafe fn bits(self, value: u8) -> &'a mut W {
        const MASK: u8 = 255;
        const OFFSET: u8 = 16;
        self.w.bits &= !((MASK as u32) << OFFSET);
        self.w.bits |= ((value & MASK) as u32) << OFFSET;
        self.w
    }
}
impl R {
    #[doc = r" Value of the register as raw bits"]
    #[inline]
    pub fn bits(&self) -> u32 {
        self.bits
    }
    #[doc = "Bits 0:7 - TSM RX Recycle Count 0"]
    #[inline]
    pub fn recycle_count0(&self) -> RECYCLE_COUNT0R {
        let bits = {
            const MASK: u8 = 255;
            const OFFSET: u8 = 0;
            ((self.bits >> OFFSET) & MASK as u32) as u8
        };
        RECYCLE_COUNT0R { bits }
    }
    #[doc = "Bits 8:15 - TSM RX Recycle Count 1"]
    #[inline]
    pub fn recycle_count1(&self) -> RECYCLE_COUNT1R {
        let bits = {
            const MASK: u8 = 255;
            const OFFSET: u8 = 8;
            ((self.bits >> OFFSET) & MASK as u32) as u8
        };
        RECYCLE_COUNT1R { bits }
    }
    #[doc = "Bits 16:23 - TSM RX Recycle Count 2"]
    #[inline]
    pub fn recycle_count2(&self) -> RECYCLE_COUNT2R {
        let bits = {
            const MASK: u8 = 255;
            const OFFSET: u8 = 16;
            ((self.bits >> OFFSET) & MASK as u32) as u8
        };
        RECYCLE_COUNT2R { bits }
    }
}
impl W {
    #[doc = r" Reset value of the register"]
    #[inline]
    pub fn reset_value() -> W {
        W { bits: 1705060 }
    }
    #[doc = r" Writes raw bits to the register"]
    #[inline]
    pub unsafe fn bits(&mut self, bits: u32) -> &mut Self {
        self.bits = bits;
        self
    }
    #[doc = "Bits 0:7 - TSM RX Recycle Count 0"]
    #[inline]
    pub fn recycle_count0(&mut self) -> _RECYCLE_COUNT0W {
        _RECYCLE_COUNT0W { w: self }
    }
    #[doc = "Bits 8:15 - TSM RX Recycle Count 1"]
    #[inline]
    pub fn recycle_count1(&mut self) -> _RECYCLE_COUNT1W {
        _RECYCLE_COUNT1W { w: self }
    }
    #[doc = "Bits 16:23 - TSM RX Recycle Count 2"]
    #[inline]
    pub fn recycle_count2(&mut self) -> _RECYCLE_COUNT2W {
        _RECYCLE_COUNT2W { w: self }
    }
}
